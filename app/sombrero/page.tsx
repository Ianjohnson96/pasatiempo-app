import ElSombrero from "@/components/sombrero/ElSombrero";

// El Sombrero — Pasatiempo Men's Club event page (static content, now a real
// React route in the app instead of a loose HTML file).
export default function SombreroPage() {
  return <ElSombrero />;
}
